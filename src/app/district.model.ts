export class District {
  private district_name: string;
  private district_id: string;

  constructor(district_name, district_id) {
    this.district_id = district_id;
    this.district_name = district_name;
  }
}
